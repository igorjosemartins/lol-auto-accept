import type { Subset } from '@cuppachino/type-space';
import type { Credentials } from '../../types/tokens.js';
export declare function createHeaders(credentials: Subset<Credentials, 'remotingAuthToken'>): {
    Authorization: string;
};
