import type { Combine } from '@cuppachino/type-space';
import type { Credentials } from '../../index.js';
import { HttpsAgent } from './agent.js';
export declare function createRequestInit<PartialCredentials extends Combine<Pick<Credentials, 'certificate' | 'remotingAuthToken'>>>({ remotingAuthToken: password, certificate }: PartialCredentials): {
    agent: HttpsAgent<string | undefined>;
    headers: {
        Authorization: string;
    };
};
