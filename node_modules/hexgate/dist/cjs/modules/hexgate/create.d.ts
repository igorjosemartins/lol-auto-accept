import type { Credentials } from '../../types/tokens.js';
import { Hexgate } from './index.js';
/** Create a new Hexgate instance */
export declare function createHexgate(credentials: Credentials): Hexgate;
